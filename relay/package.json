{
  "name": "@focus-lock/relay",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hostable sync relay: pairing handshake routing + signed session-record broadcast. No accounts, no activity logs.",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@focus-lock/shared": "*",
    "@fastify/websocket": "^11.3.0",
    "fastify": "^5.11.3"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@types/ws": "^8.5.13",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4",
    "ws": "^8.21.3"
  }
}

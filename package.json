{
  "name": "focus-lock",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-device focus enforcement system: relay + desktop + Android + iOS",
  "workspaces": [
    "shared",
    "relay",
    "desktop"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "license": "UNLICENSED"
}
